#pragma once

// Copy this file to mqtt_secrets.h and fill in the password created on the
// broker. Never commit mqtt_secrets.h.
inline constexpr char MQTT_USERNAME[] = "iot-water-level";
inline constexpr char MQTT_PASSWORD[] = "REPLACE_WITH_DEVICE_PASSWORD";
